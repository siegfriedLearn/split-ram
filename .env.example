# Credenciales públicas de Google (gratis, ver README → "Publicar tu propia instancia")
# OAuth Client ID (tipo Web, con tus URLs como orígenes autorizados)
VITE_GOOGLE_CLIENT_ID=
# API key para el selector de archivos de Google (restringida por referrer)
VITE_GOOGLE_API_KEY=
